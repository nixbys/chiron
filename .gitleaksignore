# Allowlisted gitleaks findings — one fingerprint per line.
# Add entries here for known false positives in git history that cannot be
# retroactively amended (e.g. fixtures introduced before # gitleaks:allow was added).

# jwt.io canonical demo token in test_transform_server.py. This is the public
# example payload from jwt.io, not a real credential (see the inline
# `# gitleaks:allow` on the same line in the current tree — that suppresses
# future scans of the working copy, but gitleaks also walks full git history,
# where the token appears without that comment in the commit that introduced
# it). Two fingerprints are listed because this repo has TWO distinct commit
# objects with byte-identical content introducing the same line: history
# reconstruction during the upstream-sync merges (see
# upstream_sync_progress.md in project memory) produced a different commit
# SHA on `dev` than the original commit reachable only via the `v0.3.1` tag.
# Only the `dev`-reachable SHA is what CI's checkout (fetch-depth: 0 on the
# pushed ref) actually scans, so that's the one that matters going forward;
# the tag-only SHA is kept in case that ref is ever scanned directly.
36edb229e342b627114ed20b0efee61c0feb5053:tests/mcp_servers/test_transform_server.py:jwt:86
6a7ce65492a164978d9546ef126d8a9054152107:tests/mcp_servers/test_transform_server.py:jwt:86
